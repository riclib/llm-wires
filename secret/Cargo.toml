[package]
name = "wire-secret"
version = "0.1.0"
description = "A secret that does not print itself: redacted Debug/Display, zeroed on drop, and one named call to get the bytes out"
keywords = ["secret", "redact", "zeroize", "credentials"]
categories = ["cryptography", "api-bindings"]
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
zeroize.workspace = true
